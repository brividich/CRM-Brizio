MODULO ASSET:
aumentare dimensione max file a 50MB.
inserire nell'header della pagina /assets/view/<id> uno spazio per inserir un img con la targhetta della macchina. tutti con solita dimensione. se "vuota", non si vede

MODULO ANAGRAFICA:
REPARTI: dopo la modifica della scorsa volta reparto + area aziendale, l'organigramma non riporta quanto è effettivamente cambiato. inoltre alcune viste di tabelle all'interno di tutto il modul anagrafica riportano i vecchi reparti ( mi sembra sia cambiata proprio la tabella- colonna). quindi rimuoviamo i reparti legacy e innseriamo l'acocppiata reparto + area aziendale.
FORMAZIONE: gestire meglio la gestione e la creazione dei corsi e-learning, con un setup wizard completo. dimensione max dei file 50MB
SCADENZIARIO: raggruppare per "tipo" con espansione poi dei dipendenti. per tutti gli scadenziari.
i capireparto sono sotto il caporeparto BENEDETTA BELLUCCI ( sicocme ci sono queste cose, prevedere in impostazioni - ruoli
Unire RUOLI AZIENDALI con RUOLI OPERATIVI. se vuoi ti passo l'organigramma attuale. per ogni ruolo prevedere anche il campo "certificazione di competenza" , opzionale. questo diventa un altro punto chiave del modulo anagrafica e del portale in generale in quanto poi , con quest ruoli, andremo a semplificare anche la gestione dei pemressi ACL( di questo ce ne occupiamo poi).


MODULO TIMBRI: anche qua aggiustare la colonna "reparto" e modificare quindi i filtri. 

MODULO ASSENZE: in richiesta assenza, bloccare la colonna "caporeparto" con il caporeparto ASSEGNATO al dipendente. SE, e solo SE, il caporeparto quel giorno risulta "assente", la richiesta andrà al suo supeirore( questo si potrà dedurre dalla nuova gestione dei ruoli).

MODULO REFRESH PROCEDURE:  campagna diventa sessione. Nel wizard di configurazione delle sessioni,  rendere carino il caricamento delle procedure da leggere e anche "multi scelta". nell'assegnazione degli utenti, prevedere scelta per reparto + lista + cerca

MODULO SUGGESTION CORNER E KICKOFF: in tutti i dropdown prevedere la vista di nome e cognome e non dell'username.

MODULO DIARIO PREPOSTO: usare il template del portale nei eport PDF ( devono essere tutti cosi!!)

MODULO SEGN. SICUREZZA: reparti sbagliati (riporta ancora i reparti legacy)
